import {Meta, Story, Preview} from "@storybook/addon-docs/blocks";
import MathRenderer from "../src/components/math-renderer.tsx";

<Meta title="MDX Test" component={MathRenderer} />

# Hello, world!

Here is some documentation written in MDX. The downside of MDX, is
that all code that isn't JSX has to be defined in a separate file.
The upside though is that all examples will have associated code.

<Preview>
    <Story name="Heading 1">
        <h1>Hello, world!</h1>
    </Story>
</Preview>

```js
This is a code snippet
```
